apiVersion: v1
kind: Pod
metadata:
        name: nginx
        namespace: jenkins
        labels:
                app: nginx
                tier: frontend  
spec:
        containers:
                - name: nginx
                  image: nginx
